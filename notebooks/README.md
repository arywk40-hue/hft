# Notebooks

No analytical notebooks are required by the completed implementation. The
reproducible execution path is configuration-driven Python under `src/ebx/`
and `scripts/`. This directory remains available for future presentation-only
notebooks, consistent with `architecture.md`.
